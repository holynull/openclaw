{
  "id": "omnibridge",
  "name": "Omnibridge",
  "description": "Cross-chain token swaps via Omnibridge (supports 30+ chains including BTC, ETH, BSC, TRON, Solana)",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "sourceFlag": {
        "type": "string",
        "description": "Source flag for API requests (default: catwallet_openclaw)",
        "default": "catwallet_openclaw"
      },
      "sourceType": {
        "type": "string",
        "description": "Source type for API requests (default: catwallet_openclaw)",
        "default": "catwallet_openclaw"
      },
      "equipmentNo": {
        "type": "string",
        "description": "Equipment identifier (optional)"
      }
    }
  }
}
